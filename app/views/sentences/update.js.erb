if ('<% @redirect %>' === ''){
  $("#sentence-container")
  .replaceWith("<%= j render 'sentences/shared/form', sentence: @sentence, prev_sentence: @prev_sentence, responses: @responses, result: @result %>")
} else {
  window.location = ('<%= @redirect %>')
}

